# hybrid_species_rdna_methylation_long_read
This pipeline is for tabulating per-read and per-base methylation statistics of rDNA in hybrid animal species (liger, mule, etc). Due to the large file size of alignments derived from nanopore long reads and the many small files generated in this pipeline, it is designed so that the user can partition the input read as many times as needed.
